<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.distributed.productservice.mapper.ProductMapper">

    <insert id="insert" parameterType="com.distributed.productservice.domain.ProductDO" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO products(title, sku_code, price, status)
        VALUES (#{title}, #{skuCode}, #{price}, #{status})
    </insert>

    <select id="selectById" parameterType="long" resultType="com.distributed.productservice.domain.ProductDO">
        SELECT
            id,
            title,
            sku_code AS skuCode,
            price,
            status,
            created_at AS createdAt,
            updated_at AS updatedAt
        FROM products
        WHERE id = #{id}
        LIMIT 1
    </select>

    <update id="updateSelective" parameterType="com.distributed.productservice.domain.ProductDO">
        UPDATE products
        <set>
            <if test="title != null">title = #{title},</if>
            <if test="skuCode != null">sku_code = #{skuCode},</if>
            <if test="price != null">price = #{price},</if>
            <if test="status != null">status = #{status},</if>
            updated_at = CURRENT_TIMESTAMP
        </set>
        WHERE id = #{id}
    </update>

    <sql id="whereClause">
        <where>
            <if test="keyword != null and keyword != ''">
                AND (title LIKE CONCAT('%', #{keyword}, '%') OR sku_code LIKE CONCAT('%', #{keyword}, '%'))
            </if>
            <if test="status != null">
                AND status = #{status}
            </if>
        </where>
    </sql>

    <select id="selectPage" resultType="com.distributed.productservice.domain.ProductDO">
        SELECT
            id,
            title,
            sku_code AS skuCode,
            price,
            status,
            created_at AS createdAt,
            updated_at AS updatedAt
        FROM products
        <include refid="whereClause"/>
        ORDER BY id DESC
        LIMIT #{size} OFFSET #{offset}
    </select>

    <select id="selectCount" resultType="long">
        SELECT COUNT(1)
        FROM products
        <include refid="whereClause"/>
    </select>
</mapper>
